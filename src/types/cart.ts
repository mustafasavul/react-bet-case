export interface CartItem {
  code: string;
  matchId: string;
  matchName: string;
  marketId: string;
  marketName: string;
  mbs?: number;
  oddId: string;
  oddLabel: string;
  oddValue: number;
}